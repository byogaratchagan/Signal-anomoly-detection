# Signal-anomoly-detection
This project is to find anomaly detection in embedded systems to find the failure of electronic component earlier using autoencoder algorithm which would run locally by raspberry pi pico based on the trained model
